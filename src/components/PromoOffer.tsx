import { Sparkles } from "lucide-react";

export default function PromoOffer() {
  // This component is no longer used, but we'll keep a minimal implementation
  // in case there are any references to it elsewhere in the codebase
  return null;
}
